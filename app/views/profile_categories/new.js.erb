$("#notifications").html("");
$(".modal-body-inner").html("<%= j render(:partial => "profile_categories/new_with_organization") %>");

$("#myModalLabel").html('<%= I18n.t(:new_profile_category) %>');

$("#edit_component").modal({
  keyboard: false,
  backdrop: "static"
});

$("#notifications").html("<%= j(render "layouts/notifications") %>");
