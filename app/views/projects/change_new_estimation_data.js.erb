$("#notifications").html("");

$(".modal-body-inner").html("<%=raw j render :partial => 'projects/edit_estimation_from_model', :object => @project_template %>");

$("#myModalLabel").html("<%= I18n.t(:text_create_from_model, :model_name => @project_template) %>");

$("#edit_component").modal({
    keyboard: false,
    backdrop: "static"
});

$("#notifications").html("<%= j(render 'layouts/notifications') %>");