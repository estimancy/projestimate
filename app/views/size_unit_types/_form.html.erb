<div class="tabs">
  <ul>
    <li><a href="#tabs-1"><%= I18n.t(:size_unit_type) %> </a></li>
  </ul>

  <div id="tabs-1">
    <%= simple_form_for(@size_unit_type) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :name %>
        <%= f.input :alias %>
        <%= f.input :description %>
        <%= hidden_field_tag "[size_unit_type][organization_id]", params[:organization_id] %>
      </div>

      <br>

      <div class="actions-tabs">
        <% if can? :edit_organizations, Organization %>
          <% if action_name == 'new' || action_name=='create' %>
            <%= submit_tag I18n.t('save_and_create'), :class => 'btn' %>
            <%= submit_tag I18n.t('save'), :class => 'btn' %>
          <% elsif action_name == 'edit' || action_name=='update' %>
            <%= submit_tag I18n.t('save'), :class => 'btn' %>
            <%= submit_tag I18n.t('apply'), :class => 'btn' %>
          <% end %>
        <% end %>
        <%= link_to I18n.t('back'), edit_organization_path(@organization, :anchor => 'tabs-8'), :class => 'btn' %>
      </div>
    <% end %>
  </div>
</div>
