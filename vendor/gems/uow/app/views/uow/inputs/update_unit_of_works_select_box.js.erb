$("#uow_<%= @index %>").parent().html("<%= j(select_tag "uow[#{@index}]", options_for_select(@unit_of_works.map{|uow| [uow.name, uow.id]}), :class => "uow") %>");

$('.spiner').hide();

$(document).ready(function(e){
    $("#myTable").find("tr").each(function(index){
        initialize_gross_behaviour(index);
    });
});