<h1>Listing</h1>

<table class="table">
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
  </tr>

  <% @guw_attributes.each do |guw_attribute| %>
    <tr>
      <td><%= guw_attribute.name %></td>      
      <td><%= guw_attribute.description %></td>
      <td><%= link_to 'Edit', guw.edit_guw_attribute_path(guw_attribute) %></td>
      <td><%= link_to 'Destroy', guw_attribute, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
</table>

<br />

<%= link_to 'New attribute', guw.new_guw_attribute_path %>
